info:
  name: Register User
  type: http
  seq: 3

http:
  method: POST
  url: http://127.0.0.1:8000/api/users/registerUser
  body:
    type: json
    data: |-
      {
          "userName": "admin",
          "userEmail": "admin@admin.com",
          "userPassword": "admin",
          "userPhone": "admin",
          "userAddress": "Maracaibo, Zulia"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
