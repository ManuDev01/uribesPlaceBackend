info:
  name: Update Product
  type: http
  seq: 3

http:
  method: PATCH
  url: http://127.0.0.1:8000/api/products/update/1
  body:
    type: json
    data: |-
      {
        "productDescription" : "pruebita 2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
