info:
  name: Health
  type: http
  seq: 2

http:
  method: GET
  url: uribesplacebackend-production.up.railway.app
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
