info:
  name: delete store
  type: http
  seq: 4

http:
  method: PATCH
  url: http://127.0.0.1:800/api/stores/desactivate
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
